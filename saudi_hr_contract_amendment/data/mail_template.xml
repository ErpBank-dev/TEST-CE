<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <!-- Email template contract updation -->
        <record id="hr_contract_amendment_updation_email" model="mail.template">
            <field name="name">Notification Contract Amendment Updation</field>
            <field name="model_id" ref="model_transfer_employee"/>
            <field name="email_from">${object.employee_id.company_id.email or ''}</field>
            <field name="email_to">${object.employee_id.work_email or ''}</field>
            <field name="subject">Contract Amendment</field>
            <field name="body_html"><![CDATA[
                    <p>Hello ${object.employee_id.name},</p>
                    <p>AMENDMENT TO CONTRACT Employment Agreement
                    Between your company
                    and ${object.employee_id.name} dated <b>${object.effective_date or ''}</b>.
                    </p>
                    <p>
                    The following amendments/add to above referenced contract will be made effective from <b>${object.hr_contract_id.date_start} ${object.job_id and object.job_id.name or ''}</b> in the above reference contract your company and <b>${object.employee_id.name}</b>
                    acknowledge that as of <b>${object.effective_date or ''}</b> the employee will from <b>${object.department_id.name}</b> to <b>${object.new_department_id.name}</b>
                    the employees <b>${object.job_id and object.job_id.name or ''}
                    % if ctx['grade']:
                    , ${object.grade_id and object.grade_id.name or ''}</b>
                    % endif
                     and <b>${object.department_id.name}</b> will be replaced with <b>${object.new_job_id and object.new_job_id.name or ''} ,
                     % if ctx['grade']:
                     ${object.new_grade_id and object.new_grade_id.name or ''},
                     % endif
                     ${object.new_department_id.name}</b>
                    </p>
                    <p>your current base location <b>${object.branch_id.name}</b> will change to <b>${object.new_branch_id.name}</b></p>
                    <p>Thank you,</p>
                    <p>${object.employee_id.company_id.name}</p>]]>
            </field>
            <field name="user_signature" eval="False"/>
            <field name="auto_delete" eval="False"/>
        </record>
    </data>
</odoo>